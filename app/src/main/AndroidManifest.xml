<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rozan.androidapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StyleActivity"></activity>
        <activity android:name=".FloatingActionButtonActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SpinnerActivity"></activity>
        <activity android:name=".ConstraintDesignActivity" />
        <activity android:name=".RelativeColorLayout" />
        <activity android:name=".RelativeDesignLayout" />
        <activity android:name=".RelativeLayout" />
        <activity android:name=".ImageActivity" />
        <activity android:name=".CalculatorActivity" />
        <activity android:name=".GridActivity" />
        <activity android:name=".MessageActivity" />
        <activity android:name=".RadioButtonActivity" />
        <activity android:name=".SimpleInterestActiity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>